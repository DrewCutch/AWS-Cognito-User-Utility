# AWS-Cognito-User-Utility
This console application allows one to create new accounts or login and retrieve a token.
## Configuration
To configure the cognito pool ID and client ID change the values in the app.config file.
